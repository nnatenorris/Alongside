PORT=4000
PUBLIC_BASE_URL=https://alongside.app

# Optional — without these, share creation logs the SMS to the console
# instead of sending it, so you can develop without a Twilio account.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
